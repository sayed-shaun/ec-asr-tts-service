torch>=2.1.0

# NeMo ASR toolkit — provides hishab/titu_stt_bn_fastconformer (FastConformer CTC)
nemo_toolkit[asr]>=2.0.0

# Model serving
litserve>=0.2.9

# Audio decoding/resampling
librosa>=0.10.0
soundfile>=0.12.1

# API layer
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.0
pydantic-settings>=2.2.0
python-dotenv>=1.0.0
python-multipart>=0.0.9  # required by FastAPI for UploadFile/File(...) form parsing
httpx>=0.27.0  # in-process ASGI call used by the /v1/asr/transcribe-file convenience route

# Utilities
loguru>=0.7.2
numpy>=1.24.0
requests>=2.31.0
